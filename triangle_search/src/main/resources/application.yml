server:
  port: 9007
spring:
  application:
    name: triangle‐search #指定服务名
  data:
    elasticsearch:
      cluster‐nodes: 127.0.0.1:9300

  jpa:
    show-sql: true

eureka:
  client:
    service‐url:
      defaultZone: http://localhost:8761/eureka
  instance:
    # 跨域访问
    prefer‐ip‐address: true