server:
  port: 9009
spring:
  application:
    name: triangle‐sms #指定服务名
  rabbitmq:
    host: 120.79.7.246
    username: guest
    password: guest
eureka:
  client:
    service‐url:
      defaultZone: http://localhost:8761/eureka
  instance:
    # 跨域访问
    prefer‐ip‐address: true