server:
  port: 9010

eureka:
  client:
    service‐url:
      defaultZone: http://localhost:8761/eureka
  instance:
    # 跨域访问
    prefer‐ip‐address: true
spring:
  application:
    name: triangle-recruit
